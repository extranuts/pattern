package behavioral.interpreter;


/**
 *
 * паттерн решающий часто встречающуюся, но подверженную изменениям, задачу.
 * Абстрактный базовый класс определяет метод interpret), принимающий
 * (в качестве аргумента) текущее состояние языкового потока.
 * Каждый конкретный подкласс реализует метод interpret),
 * добавляя свой вклад в процесс решения проблемы.
 *
 * */
public class InterpreterApp {

    public static void main(String[] args) {

    }
}
